node_modules/
.next/
out/
# `dist/` is the deployable source for this dependency-free static build; do not ignore it.
.vercel/
.openai/
.env
.env.*
!.env.example
coverage/
*.log
.DS_Store
Thumbs.db

# Intentionally do not ignore .agent-logs/; the assessment requires it.
